
version: '3.8'
services:
  chroma:
    image: chromadb/chroma:latest
    ports:
      - "8001:8000"
    volumes:
      - ./chroma-data:/chromadb/data
    environment:
      - CHROMA_SERVER_HTTP_PORT=8001
